This code is the frankenstein assignment
The code can create tokens from the book frankenstein and then add sucessor tokens to a token.
It is then able to form sentences.
It will form a sentence untill a sentence ends with a ? essentially making a question
It will then search for a token that ends with a ! essentially creating an answear.
The code is only able to run after the pg84.txt file has been compiled. before running you have to type in "clang @compile_flags.txt ./main.c -o main
Then you have to run the program from the terminal by typing in ./main 
